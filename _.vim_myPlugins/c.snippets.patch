--- c.snippets	2013-10-18 23:54:06.605123179 +0800
+++ _c.snippets	2013-10-19 00:03:56.527997735 +0800
@@ -65,13 +65,13 @@
 	}
 # For Loop
 snippet for
-	for (${2:i} = 0; $2 < ${1:count}; $2${3:++}) {
-		${4:/* code */}
+	for (${2:int} ${3:i} = 0; $3 < ${1:count}; $3${4:++}) {
+		${5:/* code */}
 	}
 # Custom For Loop
 snippet forr
-	for (${1:i} = ${2:0}; ${3:$1 < 10}; $1${4:++}) {
-		${5:/* code */}
+	for (${1:int} ${2:i} = ${3:0}; ${4:$2 < 10}; $2${5:++}) {
+		${6:/* code */}
 	}
 # Function
 snippet fun
